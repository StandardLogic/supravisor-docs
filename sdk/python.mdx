---
title: "Python SDK"
description: "pip install uniplex — full reference for the Python SDK."
---

# Python SDK

The `uniplex` Python library provides a clean, idiomatic interface to Supravisor's passport, gate, and audit trail systems. It handles Ed25519 cryptography, token generation, constraint evaluation, and API communication.

**Requirements:** Python 3.8+

---

## Installation

```bash
pip install uniplex
```

Install with optional extras:

```bash
# With Flask integration helpers
pip install uniplex[flask]

# With FastAPI integration helpers
pip install uniplex[fastapi]

# With async support
pip install uniplex[async]
```

---

## Quick Start

```python
from uniplex import Agent, Gate, TrustProfile

# 1. Load agent from environment
agent = Agent.from_env()

# 2. Create a gate
gate = Gate(profile=TrustProfile.L2, trusted_issuers=["issuer:mycompany.ai"])

# 3. Generate a token and authorize
token = agent.generate_token()
decision = gate.authorize(token, "web:search")

# 4. Check the result
if decision.allowed:
    print(f"Authorized! Attestation: {decision.attestation_id}")
else:
    print(f"Denied: {decision.reason}")
```

---

## Agent Class

The `Agent` class represents an AI agent with a passport and private key.

### `Agent.create(agent_id, permissions, issuer_id=None)` → `Agent`

Creates a self-issued L1 passport entirely client-side. No account needed.

```python
from uniplex import Agent

# Self-issued — no account required
agent = Agent.create(
    agent_id="my-research-agent",
    permissions=["web:search", "mcp:read"]
)

# With a specific issuer (requires issuer private key in env)
agent = Agent.create(
    agent_id="my-agent",
    permissions=["api:read"],
    issuer_id="issuer:mycompany.ai"
)

print(agent.passport_id)    # "uni_abc123"
print(agent.agent_id)       # "my-research-agent"
print(agent.permissions)    # ["web:search", "mcp:read"]
print(agent.expires_at)     # datetime(2027, 2, 24, 0, 0)
```

**Parameters:**

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `agent_id` | `str` | ✅ | Slug identifier for the agent |
| `permissions` | `list[str]` or `str` | ✅ | Permission namespaces |
| `issuer_id` | `str` | ❌ | Issuer ID (uses self-signing if omitted) |
| `expires_in` | `str` | ❌ | Duration like `"90d"`, `"1y"` (default: `"365d"`) |
| `agent_name` | `str` | ❌ | Human-readable name (default: same as `agent_id`) |

---

### `Agent(passport_id, private_key)` → `Agent`

Load an existing passport by its ID and private key.

```python
import os
from uniplex import Agent

agent = Agent(
    passport_id="uni_abc123",
    private_key=os.environ["MY_AGENT_PRIVATE_KEY"]
)
```

---

### `Agent.from_env()` → `Agent`

Load from the `UNIPLEX_PASSPORT_KEY` environment variable. This is the recommended pattern for production agents.

```python
from uniplex import Agent

agent = Agent.from_env()
# Reads UNIPLEX_PASSPORT_KEY from environment
# Raises EnvironmentError if not set
```

---

### `Agent.from_json(json_str)` → `Agent`

Load from a JSON string (from `agent.export_passport()`).

```python
from uniplex import Agent

# Save
passport_json = agent.export_passport()

# Restore
agent = Agent.from_json(passport_json)
```

---

### `agent.authorize(action, resource=None, metadata=None)` → `Decision`

Check if this agent can perform an action. Calls the configured gate.

```python
# Simple authorization
decision = agent.authorize("web:search")

# With resource context
decision = agent.authorize("files:read", resource="/project/main.py")

# With metadata (for 'required' constraints)
decision = agent.authorize(
    "payments:send",
    resource="stripe:charge_25.00",
    metadata={"user_id": "usr_123", "session_id": "sess_abc"}
)

if decision.allowed:
    proceed_with_action()
else:
    handle_denial(decision.reason)
```

---

### `agent.generate_token(nonce=None, metadata=None)` → `str`

Generate a signed passport token to pass to a gate. Tokens are single-use (L2/L3) and include a nonce for replay protection.

```python
token = agent.generate_token()
token = agent.generate_token(nonce="custom-nonce-123")
token = agent.generate_token(metadata={"session_id": "sess_abc"})
```

---

### Agent Properties

| Property | Type | Description |
|----------|------|-------------|
| `passport_id` | `str` | The passport ID (`uni_...`) |
| `agent_id` | `str` | The agent slug |
| `agent_name` | `str` | The human-readable name |
| `permissions` | `list[str]` | Granted permissions |
| `issuer_id` | `str` | The signing issuer |
| `expires_at` | `datetime` | Expiration timestamp |
| `public_key` | `str` | Base64 public key |

---

## Gate Class

The `Gate` class creates a local enforcement point. Gates verify passports and evaluate constraints in-process with no network call (except L3 revocation checks).

### `Gate(profile, trusted_issuers=None, constraints=None, gate_id=None)` → `Gate`

```python
from uniplex import Gate, TrustProfile

# L1 gate — no issuer verification
gate = Gate(profile=TrustProfile.L1)

# L2 gate — with trusted issuers and constraints
gate = Gate(
    profile=TrustProfile.L2,
    trusted_issuers=["issuer:mycompany.ai"],
    constraints={
        "max_per_day": "$50.00",
        "max_per_minute": 10,
        "domain_allowlist": "openai.com,anthropic.com",
        "read_only": False
    }
)

# Load a managed gate by ID
gate = Gate(gate_id="gate_def456", api_key="uni_xxxx")
```

---

### `gate.authorize(passport_token, action, resource=None, metadata=None)` → `Decision`

Verify a passport and evaluate constraints.

```python
decision = gate.authorize(token, "web:search")
decision = gate.authorize(token, "payments:send", resource="stripe:charge_50.00")
decision = gate.authorize(
    token, "api:read",
    metadata={"user_id": "usr_123", "cost": "2.50"}
)
```

---

### `gate.verify(passport_token)` → `VerificationResult`

Verify a passport's validity without checking a specific action. Useful for authentication without authorization.

```python
result = gate.verify(token)

print(result.valid)           # True/False
print(result.issuer_trusted)  # True/False
print(result.agent_id)        # "my-research-agent"
print(result.issuer_id)       # "issuer:mycompany.ai"
print(result.expires_at)      # datetime
print(result.permissions)     # ["web:search", "mcp:read"]
```

---

## Decision Class

Every `gate.authorize()` call returns a `Decision` object.

| Property | Type | Description |
|----------|------|-------------|
| `allowed` | `bool` | Whether the action is permitted |
| `reason` | `str` | Human-readable explanation (always present) |
| `attestation_id` | `str \| None` | ID of the signed attestation record |
| `constraints_evaluated` | `list[dict]` | Every constraint and its result |

```python
decision = gate.authorize(token, "web:search")

print(decision.allowed)          # True
print(decision.reason)           # "All constraints satisfied"
print(decision.attestation_id)   # "att_xyz789"

for constraint in decision.constraints_evaluated:
    print(f"{constraint['type']}: {constraint['result']}")
    # max_per_day: pass
    # domain_allowlist: pass
```

---

## Environment Variables

| Variable | Description |
|----------|-------------|
| `UNIPLEX_PASSPORT_KEY` | Agent's Ed25519 private key (base64). Used by `Agent.from_env()` |
| `UNIPLEX_API_KEY` | API key (`uni_xxxx`). Used by `UniplexClient` and managed gates |
| `UNIPLEX_GATE_ID` | Default gate ID. Used by `agent.authorize()` if no gate specified |
| `UNIPLEX_ISSUER_PRIVATE_KEY` | Issuer's signing key. Used for issuing new passports |

---

## Error Handling

All errors inherit from `UniplexError`.

```python
from uniplex import (
    Agent,
    PassportExpiredError,
    PassportRevokedError,
    InsufficientPermissionsError,
    RateLimitExceededError,
    SpendingLimitExceededError,
    UniplexError
)

try:
    agent = Agent.from_env()
    decision = gate.authorize(agent.generate_token(), "payments:send")
except PassportExpiredError:
    # Passport has passed its expires_at date
    new_passport = rotate_credentials()
except PassportRevokedError:
    # Passport was manually revoked
    alert_security_team()
except InsufficientPermissionsError as e:
    # Passport doesn't include the requested permission
    print(f"Missing permission: {e.required_permission}")
except RateLimitExceededError as e:
    # max_per_minute or max_per_hour exceeded
    import time
    time.sleep(e.retry_after_seconds)
except SpendingLimitExceededError as e:
    # max_per_day, max_per_action, or max_cumulative exceeded
    print(f"Limit: {e.limit}, Current: {e.current}")
except UniplexError as e:
    # Catch-all for other Supravisor errors
    print(f"Supravisor error: {e}")
```

---

## Full Example: Web Research Agent

A complete agent with spending limits, domain allowlist, and audit trail logging:

```python
import os
import logging
from typing import Optional
from uniplex import Agent, Gate, TrustProfile, RateLimitExceededError
import time

logger = logging.getLogger(__name__)

# Initialize once at startup
agent = Agent.from_env()  # reads UNIPLEX_PASSPORT_KEY
gate = Gate(
    profile=TrustProfile.L2,
    trusted_issuers=["issuer:mycompany.ai"],
    constraints={
        "max_per_day": "$20.00",
        "max_per_minute": 5,
        "max_per_action": "$2.00",
        "domain_allowlist": "google.com,bing.com,arxiv.org,wikipedia.org,pubmed.ncbi.nlm.nih.gov",
        "read_only": True,
        "no_pii_export": True
    }
)

def search_web(query: str, max_retries: int = 3) -> Optional[list]:
    """Perform a web search with Supravisor enforcement."""
    for attempt in range(max_retries):
        try:
            token = agent.generate_token(
                metadata={"query": query[:100], "session_id": os.environ.get("SESSION_ID")}
            )
            decision = gate.authorize(token, "web:search", resource=f"search:{query}")
            
            if not decision.allowed:
                logger.warning(
                    f"Search blocked: {decision.reason} "
                    f"[att:{decision.attestation_id}]"
                )
                return None
            
            # Perform the actual search
            results = perform_search_api_call(query)
            logger.info(f"Search authorized [att:{decision.attestation_id}]")
            return results
            
        except RateLimitExceededError as e:
            if attempt < max_retries - 1:
                logger.info(f"Rate limited, waiting {e.retry_after_seconds}s...")
                time.sleep(e.retry_after_seconds)
            else:
                logger.error("Rate limit exceeded after retries")
                return None
    
    return None

def read_page(url: str) -> Optional[str]:
    """Fetch a web page with Supravisor enforcement."""
    token = agent.generate_token()
    decision = gate.authorize(token, "web:read", resource=url)
    
    if not decision.allowed:
        logger.warning(f"Page read blocked: {decision.reason} (url={url})")
        return None
    
    return fetch_page_content(url)

# Check credential health at startup
days_remaining = (agent.expires_at - __import__('datetime').datetime.utcnow()).days
if days_remaining < 14:
    logger.warning(f"Passport expires in {days_remaining} days — rotate soon!")
```

---

## Next Steps

<CardGroup cols={2}>
  <Card title="TypeScript SDK" href="/sdk/typescript" icon="node-js">
    The npm equivalent for Node.js and TypeScript projects.
  </Card>
  <Card title="Gates" href="/concepts/gates" icon="shield">
    Deep dive into gate configuration options.
  </Card>
</CardGroup>
